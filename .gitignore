# Dependencies
node_modules/

# Build outputs
dist/
*.js
*.d.ts
*.js.map
*.d.ts.map
cdk.out/

# Environment configuration (IMPORTANT!)
.env
.env.*
!.env.example

# Local development vault
vault-local/

# CDK asset staging directory
.cdk.staging/
cdk.context.json

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing
coverage/

# Temporary files
*.tmp
.cache/

# Local MCP client config — pins an absolute path to this machine's checkout,
# so a committed copy would be wrong on every other machine (see ADR-001 in the
# vault: the Mac and WSL clones resolve to different paths).
.mcp.json
